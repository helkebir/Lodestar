syntax = "proto3";

package ls.proto.eigen;

message VectorXd {
    repeated double coeff = 1;
    int32 size = 2;
}

message MatrixXd {
    repeated VectorXd row = 1;
    int32 rows = 2;
    int32 cols = 3;
}